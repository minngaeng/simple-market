const Header = () => {
    return (
        <header
            style={{
                width: '100%',
                borderRadius: '10px',
                border: '1px solid white',
            }}
        >
            <h1>{`<Header />`}</h1>
        </header>
    );
};

export default Header;
