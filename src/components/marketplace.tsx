import PaginationPage from './paginationPage';

const Marketplace = () => {
    return (
        <div>
            <h1>Marketplace</h1>
            <PaginationPage />
        </div>
    );
};

export default Marketplace;
